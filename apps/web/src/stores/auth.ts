import {defineStore} from 'pinia';import {api} from '@/lib/api';
export const useAuthStore=defineStore('auth',{state:()=>({user:null as any,ready:false}),getters:{authenticated:s=>!!s.user,isAdmin:s=>s.user?.roles?.some((r:any)=>r.name==='Administrador')},actions:{async login(email:string,password:string){const {data}=await api.post('/auth/login',{email,password,device_name:'web'});localStorage.setItem('token',data.token);this.user=data.user},async restore(){if(!localStorage.getItem('token')){this.ready=true;return}try{this.user=(await api.get('/auth/me')).data}catch{localStorage.removeItem('token')}finally{this.ready=true}},async logout(){try{await api.post('/auth/logout')}finally{localStorage.removeItem('token');this.user=null}}}});
