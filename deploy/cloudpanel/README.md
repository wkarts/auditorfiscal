# CloudPanel

Use a stack Docker com `APP_HTTP_BIND=127.0.0.1`, `APP_HTTP_PORT=8080` e
`COMPOSE_PROFILES=`. No CloudPanel, crie um Reverse Proxy para
`http://127.0.0.1:8080`. Consulte `docs/deploy/cloudpanel.md`.
